import { MyownuppercasePipe } from './myownuppercase.pipe';

describe('MyownuppercasePipe', () => {
  it('create an instance', () => {
    const pipe = new MyownuppercasePipe();
    expect(pipe).toBeTruthy();
  });
});
